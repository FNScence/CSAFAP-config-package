// ======================== CS as fast as possible - Config Package (version 3.0) =========================
	// Written by ".FNScence" (youtube.com/c/CSasfastaspossibleFNScence)
	// Edit any keybinds to customize to your preference. To disable a feature, add "//" at the start of it's line.

//   ❗❗❗  EDIT NEXT LINE  ▼  to use YOUR own SENSITIVITY ❗❗❗
alias resetsens "sensitivity 1.0 ; m_yaw 0.022 ; sensitivity_y_scale 1" 	// ◄ SET YOUR PLAYING SENS HERE	(default 1.0 and 0.022 and 1)

// =================== BASE KEYBINDS =======================================
bind						"M"			+map_action			// ◄ Key bind for MAP SELECTION + PRACTICE MODE (default M)
bind						"J"			+T_action			// ◄ Key bind for T SMOKES (default J)
bind						"H"			+CT_action			// ◄ Key bind for CT SMOKES (default H)

//bind 						"J" 		+fAC_T_action		// ◄ Key bind for T SMOKES working on faceit (default J)
//bind						"H"			+fAC_CT_action		// ◄ Key bind for CT SMOKES working on faceit (default H)

bind						"K"			+movement_action	// ◄ Key bind for SNAP-TAP and VIEWMODEL (default K)

// =================== AveYo's binds =======================================
// ⚠️ Launch option required:   -testscript "../../csgo/cfg/CSAFAP/addons/.vtest"
bind					  "space"		+JumpThrow			// ◄ Key bind for   jumpthrow (just like in CSGO)
bind						"N"			+WJumpThrow			// ◄ Key bind for W-jumpthrow (just like in CSGO)
//bind					 "CAPSLOCK"		+CrouchJump			// ◄ Key bind for crouch-jump (just like in CSGO)
//bind						"Z"			+BH 				// ◄ Key bind for auto-bhop (tap for jumpthrow bind)

			bind MOUSE1 +M1; bind MOUSE2 +M2				// ◄ Requirements for jumpthrows. If you use non-default buttons, change here.

// ========== Snap-Tap / Null binds at game launch ============
//bind W +nullW; bind A +nullA; bind S +nullS; bind D +nullD	// ◄ Null binds (ticker version, needs launch option from line 19)
//exec CSAFAP/movement/nullWASD								// ◄ Regular Null binds
//exec CSAFAP/movement/default								// ◄ Default movement binds for WASD

// ================ Viewmodel Settings =====================================
	// Options: "righthanded", "lefthanded", "kniferight", "knifeleft"
//alias set_viewmodel "exec CSAFAP/movement/viewmodel/kniferight"

	// The following keybinds are only needed if you plan on using the viewmodel options above:
//bind						"1"			eq_slot1			// ◄ Slot1 / primary gun
//bind						"2"			eq_slot2			// ◄ Slot2 / pistol
//bind						"3"			eq_slot3			// ◄ Slot3 / knife
//bind						"4"			eq_slot4			// ◄ Slot4 / grenade cycle
//bind						"5"			eq_slot5			// ◄ Slot5 / bomb
//bind						"Q"			eq_lastinv			// ◄ Last used weapon
//bind						"C"			eq_slot6			// ◄ HE grenade
//bind						"F"			eq_slot7			// ◄ Flashbang
//bind						"G"			eq_slot8			// ◄ Smoke grenade
//bind						"Z"			eq_slot9			// ◄ Decoy grenade
//bind						"V"			eq_slot10			// ◄ Molotov/Incendiary

//bind					"mwheeldown"	eq_invnext			// ◄ Cycle Weapons
//bind					 "mwheelup"		eq_invprev			// ◄ Cycle Weapons	



//\/\/\/\/\/\/\/\/\===========  DON'T EDIT THE REMAINING LINES   ===========/\/\/\/\/\/\/\/\
exec CSAFAP/addons/loader	// ◄ DO NOT EDIT! Calling the main logic of the cfg-framework.
