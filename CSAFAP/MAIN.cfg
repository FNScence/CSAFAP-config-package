// ======================== CS as fast as possible - Config Package (version 1.5) =========================
// Written by ".FNScence" (youtube.com/c/CSasfastaspossibleFNScence)
// Edit any keybinds to customize to your preference. To disable a feature, add "// " at the start of it's line.
// ======================================= KEYBINDS ========================================
//     ❗❗❗  EDIT NEXT LINE  ▼  to use YOUR own SENSITIVITY ❗❗❗
alias resetsens "sensitivity 0.7 ; m_yaw 0.022" 							// ◄ SET YOUR PLAYING SENS HERE	(default 0.7 and 0.022)

bind 						 "N"	"+WJT"									// ◄ Key bind for desubticked W-jumpthrow (default N)
bind 					   "space"	"+JT"									// ◄ Key bind for desubticked   jumpthrow (default space)

alias smoke_hotkey1		"bind J +smoke_wheel_1"								// ◄ Key bind for SMOKES (default J)
alias smoke_hotkey2		"bind J +smoke_wheel_2"								// Insert same key as line above (default J)

alias movement_hotkey1	"bind K +move_wheel_1"								// ◄ Key bind for MOVEMENT CONFIGS (default K)
alias movement_hotkey2	"bind K +move_wheel_2"								// Insert same key as line above (default K)

alias flash_hotkey1		"bind H +flash_wheel_1"								// ◄ Key bind for INSTANT FLASHES (default H)
alias flash_hotkey2		"bind H +flash_wheel_2"								// Insert same key as line above (default H)

// ============ Keybinds of Advanced Features/Addons ============
bind 						 "E"        "+def"								// ◄ Shooting while defusing stopps defuse (and starts defusing as soon as you stop shooting again)
bind 					  "mwheeldown"  "+jump_"							// ◄ De-subticked jump
bind						 "R"		"+reload_throw"						// ◄ Release middle click nades using R, as well as reloading
bind 					  "capslock"    "+LJ"								// ◄ Key bind for crouch/duck-jump (aka long-jump)
// bind 					   "mouse4"     "+JB"								// ◄ Key bind for bug-jump
// bind 						"ctrl"      "+duck_"							// ◄ De-subticked crouch
// bind 						"shift" 	"+walk_"							// ◄ De-subticked walk
// bind 						 "W"        "+forward_"							// ◄ De-subticked forward movement
// bind 						 "A" 		"+left_"							// ◄ De-subticked left movement
// bind 						 "S" 		"+back_"							// ◄ De-subticked backwards movement
// bind 						 "D"		"+right_"							// ◄ De-subticked right movement
// bind 						 "L"	    "+default_movement"					// ◄ Key bind for default movement (reset after using strafing configs)

// ============ Set your default movement configuration at game launch  ============
// Remove the double slashes at the start of the line you want to use:
alias Set_movement "exec CSAFAP/movement/movement_default"					// ◄ Default movement
// alias Set_movement "exec CSAFAP/movement/movement_autostopAD"				// ◄ Auto counter-strafe on A+D
// alias Set_movement "exec CSAFAP/movement/movement_autonullAD"				// ◄ Auto counter-strafe and nullbinds on A+D
// alias Set_movement "exec CSAFAP/movement/movement_autostopWS_nullAD"			// ◄ Auto counter-strafe on W+S and nullbinds on A+D
// alias Set_movement "exec CSAFAP/movement/movement_nullAD"					// ◄ Nullbinds on A+D
// alias Set_movement "exec CSAFAP/movement/movement_autostopWASD"				// ◄ Auto counter-strafe on W+A+S+D



//\/\/\/\/\/\/\/\/\===========  DON'T EDIT THE REMAINING LINES   ===========/\/\/\/\/\/\/\/\
exec CSAFAP/logic															// calling the main logic of the cfg-framework
