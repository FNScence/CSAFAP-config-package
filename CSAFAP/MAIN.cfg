// ======================== CS as fast as possible - Config Package (version 3.4) =========================
	// Written by ".FNScence" (youtube.com/c/CSasfastaspossibleFNScence)
	// Edit any keybinds to customize to your preference. To disable a feature, add "//" at the start of it's line.

//   ❗❗❗  EDIT NEXT LINE  ▼  to use YOUR own SENSITIVITY ❗❗❗
alias resetsens "sensitivity 1.0 ; m_yaw 0.022 ; sensitivity_y_scale 1" 	// ◄ SET YOUR PLAYING SENS HERE	(default 1.0 and 0.022 and 1)

// =================== BASE KEYBINDS ========================================
bind						"M"			+map_action			// ◄ Key bind for MAP SELECTION + PRACTICE MODE (default M)
bind						"J"			+T_action			// ◄ Key bind for T SMOKES (default J)
bind						"H"			+CT_action			// ◄ Key bind for CT SMOKES (default H)

//bind 						"J" 		+fAC_T_action		// ◄ Key bind for T SMOKES working on faceit (default J)
//bind						"H"			+fAC_CT_action		// ◄ Key bind for CT SMOKES working on faceit (default H)

bind						"K"			+crosshair_action	// ◄ Key bind for PRO-CROSSHAIRS, BETTER FOLLOW-RECOIL, RAPID FIRE, SNAP-TAP (default K)

// =================== AveYo's binds ========================================
// ⚠️ Launch option required:   -testscript "../../csgo/cfg/CSAFAP/addons/.vtest"
bind					  "space"		+JumpThrow			// ◄ Key bind for   jumpthrow (just like in CSGO)
bind						"N"			+WJumpThrow			// ◄ Key bind for W-jumpthrow (just like in CSGO)
//bind					 "CAPSLOCK"		+CrouchJump			// ◄ Key bind for crouch-jump (just like in CSGO)
//bind					   "alt"		toggle_rapid		// ◄ Key bind to toggle ON/OFF rapid fire and/or follow recoil

			  bind MOUSE1 +M1; bind MOUSE2 +M2				// ◄ Requirements for this section. If you use non-default buttons, change here.

// ========== Enable settings at game launch (simply remove //) ==============
//                exec CSAFAP/movement/nullWASD				// ◄ Null binds (ticker version, needs launch option from line 19)
// 			  alias load_follow_recoil load_rpd				// ◄ Rapid Fire
// 			  alias load_follow_recoil load_flw				// ◄ Better Follow Recoil
// 			  alias load_follow_recoil load_rpdflw			// ◄ Better Follow Recoil + Rapid Fire

// ========== To fix instant releasing nades when using follow recoil, bind them here ============
// ⚠️ Requirement for follow-recoil bind ▼
//bind 1            "ef_slot1"      //primary
//bind 2            "ef_slot2"      //secondary
//bind 3            "ef_slot3"      //melee
//bind 4            "ef_slot4"		//cycle nades
//bind 5            "ef_slot5"      //bomb
//bind c            "ef_slot6"      //HE grenade
//bind f            "ef_slot7"      //flash
//bind g            "ef_slot8"      //smoke
//bind z            "ef_slot9" 		//decoy
//bind scancode100  "ef_slot9"      //decoy
//bind v            "ef_slot10"     //molotov/incendiary
//bind q            "ef_lastinv"    //quickswitch

// =================== Crosshair & Viewmodel ================================
// ⚠️ Requirement for crosshair section: INSERT your crosshair and viewmodel settings here ▼
alias reset_crosshair "cl_crosshairstyle 4; cl_crosshair_recoil 0; cl_crosshairdot 0; cl_crosshairsize 1.8; cl_crosshairthickness 1; cl_crosshairgap -2.3; cl_crosshair_drawoutline 0; cl_crosshair_outlinethickness 1; cl_crosshaircolor 5; cl_crosshaircolor_b 0; cl_crosshaircolor_r 255; cl_crosshaircolor_g 255; cl_crosshairusealpha 1; cl_crosshairalpha 255; cl_crosshair_t 0; cl_crosshairgap_useweaponvalue 0; cl_crosshairgap_useweaponvalue 0; cl_crosshair_sniper_width 2"
alias reset_viewmodel "viewmodel_presetpos 1;viewmodel_fov 90;viewmodel_offset_x 1;viewmodel_offset_y 2;viewmodel_offset_z -1.5;"















//\/\/\/\/\/\/\/\/\===========  DON'T EDIT THE REMAINING LINES   ===========/\/\/\/\/\/\/\/\
exec CSAFAP/addons/loader	// ◄ DO NOT EDIT! Calling the main logic of the cfg-framework.

